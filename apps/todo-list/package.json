{
  "name": "@dunamismax/todo-list-app",
  "version": "2.0.0",
  "description": "Full-stack todo list application built with Fastify, vanilla HTML, and MongoDB",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "clean": "rm -rf dist",
    "preview": "pnpm build && pnpm start"
  },
  "dependencies": {
    "fastify": "^5.4.0",
    "@fastify/static": "^8.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "mongodb": "^6.18.0",
    "@vinejs/vine": "^3.0.1",
    "dotenv": "^17.2.0",
    "@dunamismax/utils": "workspace:*",
    "@dunamismax/flare-router": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
